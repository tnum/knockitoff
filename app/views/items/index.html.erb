<div class="todo-list">

<% if current_user %>

  <%=link_to "Add item", new_item_path %>

  <div class="items">
    <%= render partial: 'form', collection: @items, as: :item %>
  </div>

<% end %>

</div>